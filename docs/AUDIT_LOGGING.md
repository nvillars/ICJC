```markdown
# Audit logging (Bitácora) - ICJC

AuditLog model captures user actions and before/after snapshots. Log on:
- Create/Update/Delete/Publish/Unpublish actions
- Login/Logout attempts
- Sync operations (facebook)

Provide UI with filters by date, user, entity, and action. Show diff between before and after. Allow export CSV/JSON.

Retention: keep 12 months by default; archive older logs.
```
